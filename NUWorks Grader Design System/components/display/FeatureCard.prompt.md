Marketing feature card with a tinted icon chip — pass `tone="moss"` to alternate chip colors; icons are 2px-stroke inline SVGs.

```jsx
<FeatureCard icon={<FileIcon/>} tone="moss" title="Resume matching">
  Upload once. Every posting is scored against the skills it finds.
</FeatureCard>
```
