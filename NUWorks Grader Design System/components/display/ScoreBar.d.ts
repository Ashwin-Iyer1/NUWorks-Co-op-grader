/** 5px rounded match-score bar, colored by the same high/medium/low thresholds as Badge (moss/amber/brick). */
export interface ScoreBarProps {
  /** 0-100 */
  score?: number;
}
export declare function ScoreBar(props: ScoreBarProps): JSX.Element;
