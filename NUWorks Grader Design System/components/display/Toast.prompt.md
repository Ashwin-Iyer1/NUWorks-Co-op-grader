Toast — fixed bottom-right notification; `tone` gives tinted success/warning/error variants.

```jsx
<div style={{position:"fixed",bottom:24,right:24}}><Toast show tone="success">Saved 12 jobs</Toast></div>
```
