Brand button — solid clay primary, quiet outlined secondary, text ghost, tinted semantic danger/success. Sans 600, sentence case, 8px radius.

```jsx
<Button variant="primary" size="lg">Fetch & Analyze</Button>
<Button variant="secondary">Open NUWorks</Button>
<Button variant="danger" size="sm">Stop</Button>
```

Variants: primary (clay, hover deepens + soft shadow, active scales 0.98), secondary (outline, hover strengthens border), ghost, danger, success. Sizes sm/md/lg. `disabled` renders at 0.45 opacity.
