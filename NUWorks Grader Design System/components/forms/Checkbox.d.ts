/** Native checkbox with clay accent-color and sans label. */
export interface CheckboxProps {
  label?: string;
  checked?: boolean;
  onChange?: (e: any) => void;
}
export declare function Checkbox(props: CheckboxProps): JSX.Element;
