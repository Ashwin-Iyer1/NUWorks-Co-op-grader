Labeled form input — sentence-case label, white field, clay border + soft glow ring on focus.

```jsx
<Input label="Jobs to scrape" type="number" value={200} />
<Input label="Min Match Score" hint="A good score is around 40-50%" />
```
