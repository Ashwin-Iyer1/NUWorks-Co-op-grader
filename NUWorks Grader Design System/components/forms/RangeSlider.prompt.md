Range slider — round clay thumb on a soft rounded track; value renders inside the label.

```jsx
<RangeSlider label="Min Match Score" value={40} step={10} hint="A good score is around 40-50%" onChange={fn} />
```
