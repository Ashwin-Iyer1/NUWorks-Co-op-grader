# Marketing Site UI kit

The "Natural Professional" landing page: sticky blurred nav, split hero (serif headline with italic clay accent, real product screenshot), tinted feature band with icon-chip cards, 3-step getting started, closing CTA, footer. Light (warm linen) + dark ("ember by lamplight") via the nav toggle.

Content matches the real site's messaging; the terminal-style original lives on the chrome-extension-site branch for reference.